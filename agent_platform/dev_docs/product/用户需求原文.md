# 用户需求原文

```yaml
status: source
updated: 2026-09-05
default_agent_context: false
scope: Agent Platform 产品定义、任务图、验证、文档结构与工作 Agent 入口的用户原始输入
```

本文只保存用户原始表达，供产品边界复核、目标解释和需求冲突处理。它不是当前产品契约，也不代替 [PRODUCT](../../PRODUCT.md)、[ARCHITECTURE](../../ARCHITECTURE.md) 或当前 Ticket。

除标题、来源和换行外，下面不加入 Assistant 的解释。口语、错别字和未定问题按原消息保留。

本文件只覆盖用户发言摘录。完整推导还需读取 [需求来源与推导索引](README.md) 及四份双方对话快照；Assistant 建议和事实断言不因归档而成为已接受规范。

## 来源索引

| ID    | 来源                                                          | 主题                                              |
| ----- | ------------------------------------------------------------- | ------------------------------------------------- |
| U-001 | `chatgpt-conversation://6a9920ea-e74c-83ec-86c5-5c18441d40f1` | Todo 与真实 Task State                            |
| U-002 | `chatgpt-conversation://6a97dcb1-7848-83ec-a858-12a37e194240` | 数据面、控制面、编排、展示与 Harness              |
| U-003 | `chatgpt-conversation://6a9a3def-c508-83ec-a157-7b31fe82d840` | Secretary、多 Workspace、多 Agent、变更与优化目标 |
| U-004 | `chatgpt-conversation://6a9a674c-8798-83ec-ad87-d121bb66bf8b` | 二维任务图、静态分析、Agent Review 与并行         |
| U-005 | 当前任务                                                      | Agent、系统与人的验证边界                         |
| U-006 | 当前任务                                                      | 架构复盘、长期漂移与空间/时间视图                 |
| U-007 | 当前任务                                                      | Plane→Module→DAG→Ticket 的文档与开发方式          |
| U-008 | 当前任务                                                      | 独立产品、重写与旧稿归档                          |
| U-009 | 当前任务                                                      | 工作子 Agent 入口、需求原文与图形结构             |

## U-001：Todo 与真实进度

```text
在不管是在哪个 agent 里面，都会有 agent 做出来一个 To-do List。但是这个 To-do List，它基本上来说，在工作期间不会随着工作阶段的推进而勾掉 To-do List 这种情况发生。一般都是在任务的结尾，就是整个任务都做完了，它才把这个 To-do List 给勾划为完成状态。在这个现象情况下，这个 To-do List 是必要的吗？因为看起来模型 Agent 都不怎么遵守它自己设下的 To-do List。然后它的本质是什么？就是它在代码当中，它是如何实现的？然后模型是如何看到这个 To-do List 的？以及对于这种现象，有什么方法来保证模型是每完成一个状态之后就勾一下 To-do List 的。
```

## U-002：总体架构、目录与开发流程

```text
根据最近的那些对话，以及这些，看一下我该如何构建计划图，数据面/控制面/编排/展示的架构
```

```text
https://openai.com/zh-Hans-CN/index/harness-engineering/ 看一下它再给一下文件夹结构以及架构图，以及具体实现开发的流程
```

## U-003：Human Collaboration、多 Workspace 与产品优先级

```text
我现在梳理一下我的想法，首先对于第一个问题，Secretary是否只响应命令查询，或者允许主动规划、提醒和打断。首先我们要明确 Secretary 的目标是什么，它的目标是为了人和多 Agents 能更好地协作。那么人需要掌握的是什么呢？人需要的是对于状态的掌握，以及把应该由人来解决的信息，一些技术上的 trade-off，工程上的 trade-off，一些模型本身可能不太了解到的真实业务信息，或者模型本身不太清楚的，就是系统已经相对地比较通用了，但是还有一些专门的信息，Agents 还不是很清楚的，要人来进行确认的这些状态，交给人来叙述说明。然后同时让模型能够尽快地跟上人类的思路，按照人类的思路来进行执行，或者说人类有什么不太懂的，可以马上问一下 Agents。这样的话就是这个 Secretary 的需求。因此默认只响应命令查询，还是允许主动规划、提醒和打断？我觉得应该是允许提醒和打断，以及讨论。它就相当于一个面板，可以切换多种不同的需求。它不一定只是一个对话窗口，可以是显示当下项目状态进行情况，可以是显示多 Agents 的分工，有哪些 Agents 被分出来了。然后它可以像，我也不知道怎么样，就是像监工面板，或者说仪表盘一样地看到我们一个 Agent 当下在做什么。然后主对话窗口则是一个实时的任务派发以及多任务的返回结果反馈的东西。就是怎么说呢？我是这么理解的，一个是人可以实时提出任何需求，然后这个需求可以被分配到合适的 Agents 里面进行解答，就是组装成一个好的 context，然后进行解答。这个过程中由于涉及到实际的工程的理解，因此可能也会根据不同的模型的标签、 context 等东西来选择某个正在工作中的 Agent，创建一个副本，来不打断工作的情况下来回答这个问题。这也是有可能的。就是，然后是一些静态的工具的调用，比如说代码分析啊，什么什么东西的调用，人也可以调用。然后是提醒，提醒的话就相当于是被，其实现在 Codex 也有这个功能，就是把要编辑的消息等到它下一次，或者当前工具调用完之后，或者思维链思考完之后，立马再插入。这种也是可以的，也是需要可以的。总之就是让人能够更加理解当下状态的情况，不仅仅是项目的状态，也是 Agents 工作的状态。同时也需要一个类似参谋的东西，让人能够更好地进行下构设计，更好地利用当前信息。这是第一个。然后是第二个首版是否严格限定一个项目和一个 workspace？这肯定不行啊。你如果能够创建一个 workspace 的话，那你就应该可以掌握多个 workspace。每一个 workspace 都可以有多个 Agents，这是肯定的。我不太清楚这个严格限定一个项目和一个 workspace 是什么意思。应该是每一个项目、每一个 workspace 都有它当下的那些状态的记录，Agents 记录等等。它可以被冻结，可以被保存为临时的快照，还是什么东西。然后两个不同的 Agents 可不可以交互呢？不同的项目中的 Agents 可不可以交互？我觉得应该是暂时先不要加入，暂时先不要加入。然后第三个，MVP 是否必须展示两个并行 worker，还是单 worker 加可替换恢复即可？这个不太清楚。这个我不太清楚，你给我解释一下。第四个，可版是 coding first，还是必须从领域模型开始又只是非编码任务。对于我来说，Agents 一个很重要的功能就是一切都可以通过代码来描述。比如说生图，比如说看图，比如说就是语言即世界这个概念。因此我觉得 coding first 的概念迁移到其他的领域，无非就是 tool 设计的不同。因为我觉得先规划后执行的软件工程思路，在工程方面应该是比较通用的。当然了，情感陪伴啊，文档读取啊，什么等可能不太行。软件工程是常文编写，这个确实可能是架构不太合适，但是我们的多 Agents 调度，设计角色，设计状态等等的应该是可以合适的。现在就先是 coding first 吧，因为不同领域的都需要不同领域的 tool，以及相应的角色设计等等。不过当下的这个一个状态实现所有任务，应该是都是通用的。然后第五个，用户修改目标是默认 cancel，就 run，still running，还是等安全点重启。它的意思是说，当我的目标变更的时候，要把旧的 run 和 cancel或者 steal掉吗？首先要理解用户为什么会修改目标，用户修改目标是在哪个地方进行修改。你先说一下你的理解。我的理解是它应该是在 Secretary 那个常对话窗口加上仪表盘表示状态的地方，通过对话窗口或者其他的工具。就比如说专门点击某个 Agents 的图形化的东西，然后提出修改，或者之类的。这个情况下应该先停一下，然后有一个秘书团的东西，Agents 来和人进行确认，确认之后再进行修改。当然了，也可以有不同的模式，就比如说直接放行什么的。然后也就是说最终要不要直接开始修改这个概念，其实还是取决于你怎么做 Secretary。因为你任务分发都是从 Secretary 进行分发的。你要修改目标，那你肯定想要更改当下怎么做任务，那你就只能通过 Secretary。所以说这个修改目标的时候，肯定是当下的状态先停止，先保存。当然了，如果是一些正在进行的命令行啊，正在执行的一些程序的操作的话，这个可能是需要根据不同的调用工具来进行考量的，以保证安全退出。然后 blocked, deferred, cancelled 和 option task 对Go完成的政策是什么意思？这个什么意思？这个是 task graph 不太清楚。然后验证无法机械完成，reviewer 用户所以拥有最终裁决权。什么叫无法机械完成？你如果说软件工程的测试的话，有单元测试和运行，就全量运行测试。全量运行测试你肯定也可以完成啊。你为什么不能完成呢？我不太理解。你的意思是说模型跑了很久，发现这个目标是达不到的吗？那这时候你就先跟用户进行反馈之后，再让用户来搞啊，或者是说语义说明，还是说。 然后是手板最优先优化的是长期连续性、完成速度、Token成本还是并行吞吐。首先要优化的是长期连续性以及对于状态的把控，就是你整个逻辑要跑通，任务可以被拆得够细够清楚，完成各个场景的适配。同时你的完成速度不能低，你完成速度低了之后，测试的效率就很低，就不利于长期改进。Token成本和并行的吞吐，这是后面再进行修改，因为我觉得Token成本和并行吞吐，Token成本是架构上其实就要有一定的考量，然后它也跟缓存命中又跟 Context 管理有关，所以说这个是需要考虑一下，是需要考虑一下。但是优先以我们的最新的目标，就是一个窗口一个仪表盘，同时任务拆得够仔细来为基础，也可以设置一些策略来判断分得好不好。因为我觉得如果任务分得够细够好的话，Token成本会自然降下来的，因为模型真正工作的好的就是在前面50%的上下文窗口，或者说256K，甚至说256K只要四分之一的窗口，下才能工作得比较好。所以说如果你的任务拆分得不够好，导致了超出50%的上下文，单个 A 检测，那么就说明你的成本上去的同时效用也不是很好，所以说我觉得 Token 成本其实和最终的完成效率其实不是一个相反的东西，就是只要你的长期连续性、完成速度以及你的架构设计符合我们的目标，那么 Token 成本会自然降下来的。然后大概就是我的理解，就是这样子。
```

## U-004：二维任务图与验证实现

```text
我觉得你的理解有个问题，你从始至终没有谈怎么实现。
你觉得技术上来说，这个 task graph 该如何来判定呢？一种是通过跑测试，就是你预先定义好测试之后再来进行一个实现；一种是由 agent 来审计，来报告是否完成。哪一个才是真正合理的呢？或者说是我们这个方案里面需要的？&#x20;
肯定是先是静态的代码分析之后，再加上 agent 来判断语义。我觉得是这样子的。就比如说一个 AST 语法树之类的东西，来针对某个模块，因为你是拆分成模块来实现了，就是 task 吗。 然后我觉得你像你这种 task 有多阶段的，然后每一个阶段依赖前一个阶段的，我觉得其实是相对不太好的。为什么呢？因为你这个好像不能实现多 agent 并行啊，不能实现多 agent 并行，其实你的效率没有多大的提高。
是的，它事实上它的并行有多种，就是每一个阶段都应该有多个模块一起在实现，这样才能实现多 agent 的一个东西。因此这里有两个，一个是时间线上面的一个阶段有没有完成，一个是空间上面的一些一个模块有没有写完成。
&#x20;当然最终到实际执行，就是看某一个模块有没有执行完成。这里我觉得应该是用一个静态语法分析的东西来进行初步判断，初步判断之后，然后让 agent 来进行判断。因为这比重新再写一个测试接口的代码要快很多，要快很多。&#x20;

当然你也可以一直坚持每一个模块都坚持写一个测试的模块，但是我觉得这太麻烦了，而且 OpenAI 推荐实践应该也没有说这个东西。&#x20;

然后就到了 agent，那么这个审查的 agent 它需要有什么信息context来构成呢？我的最初的想法是架构图加上需要检查的要点，但检查的要点是怎么来的呢？ 这个可以由工作的 agent 自行来汇报它自己做的哪些问题，然后再根据架构图加上这个 agent 自己的报告，然后针对性地派一个 agent。这个 agent 在读取之前静态语法分析树的结果，因为你根据这个类似 code graph，也类似于 AST，你就大概地对这个模块里面的代码有个比较清晰的了解。然后再确定好各个动态运行时可能存在的业务情况之后，再对于每一个模块进行分析，就大概差不多了。这是模块内部的。

然后除了模块内部的，还有模块间的，模块间的一般是由提前写好的架构来确定。然后是在动态的测试当中暴露出来一些问题。当然也可以派一个 agent 去审查。不过这样的话审查又会成为一个开发速度的一瓶颈。最好就是一开始架构设计的时候就已经确定好边界。

然后大概就这些，我的理解就这些。你把这些我的理解再结合一下 OpenAI 的实践，就是我先前好像提到过的一个实践。你再看一看怎么搞。
```

## U-005：Agent、系统与人的验证边界

```text
多 Agent 是否作为 MVP 门禁：需要。
```

```text
在这里，我的理解是，你的这个问题指的是哪些属于 Agent 的系统负责，哪些属于用户的负责。然后你的意思是认为，是产品体验、真实业务、未来的架构、Trade-off 等等不太确定的，更偏向语义方面的，是由人类来决定。但是我要说，如果这些东西在刚开始项目生命周期初期的时候，我们就已经确定好了边界，那么这些问题其实只能算是非常非常小的。是的，是非常非常小的。这也是为什么程序员还能够存在的原因，就是他们需要有 Trade-off 的抉择，以及真实业务的落地。嗯，然后你认为每条验收条件预先声明验证方式，事实上根据我们先前的定义，就是一个产品的功能在我们设计好之后，执行好之后，我们设计的全量测试应该是所有的都包含了，除了一些 Agents，或者说大模型不能接受的输入，比如说，比如说图片的审美、用户的反馈，还有什么，还有就是未来架构的变动。当然未来架构的变动，这个可以由人类和大模型一起探讨。大模型本身其实有相关的实践的功能。大概就是这样。然后生产环境或第三方系统是否真的正常，这个其实现代编程应该有比较好的实践了，比如说 Docker 啊，这些沙箱技术啊，这些东西，应该不是问题。你这个还是本质上还是有哪些是人类决定，哪些是 Agents，或者说 Agent 框架来决定的东西。不过你说的这些明确进行明确标注，其实也可以。但是我觉得标注太多可能会污染 Agents 的上下文，反而不太好。大概就是这样。
```

## U-006：架构复盘、长期漂移与空间/时间视图

```text
不过随着开发的进行（一般会很快），需要考虑到复盘与小更新的需求，事实上，架构如何称之为好，就是抽象层级能否适应需求以及生命周期的管理，扩展性，可读性，是否容易协作。这始终是一个难题，不过我们可以借助openai我之前提到的那个blog https://openai.com/zh-Hans-CN/index/harness-engineering/，以及matt-skill进行实现。通过约束避免长时任务漂移导致的质量下降。

因此，这个应该还行。架构确定时的验证应该是机械化的，架构相关（其它模块的耦合以及实现后的性能没对上或者逻辑/权限冲突等语义上的）就需要agent参与，汇报给人类并提出解决思路，以及引导人类进行回忆，决策（人的注意力有限）
```

```text
就是当下空间上的有哪些与时间/生命周期上的管理，对吗？
```

## U-007：Plane、Module、DAG 与 Ticket

```text
你现在完成了产品定义，完成了 sealing 的 planning，完成了总体架构和任务图与判断的这个东西。但是我觉得这个太多了，太多了。你应该看到了我之前给你的那个 OpenAI 的那个博客，你应该历史里面应该有没有，有没有？我看一下。我看一下。啊，对呀，就是那个博客，它那里面以及那个 MapSQL 应该说了，应该要根据 DAG 依赖模块，就是先把模块确定了，哪些模块，先抽象面，现在是数据面、控制面这些面嘛。然后每一个面由相关的模块组成，然后再用 DAG 的关系构建依赖，然后再用这些东西根据开发的流程构建 tickets。然后每一个 agent 都只完成那几个模块，几个面。然后最终再来做交互交互的东西。这个时候可能污染就相对较少一些，反正就减少长下指令带来的污染。所以说你到时候搞文档可能还要搞得再细一些。
```

## U-008：作为独立产品重写并归档

```text
帮助我写一下M7的文档？或者说新建一个阶段的？这几乎是另一个产品。
```

```text
进行重构，不能说重构吧，进行再写。如果你觉得前面的文档需要归档的话，那就归档。我也不太清楚怎么归档，或者说不用归档。
```

## U-009：工作 Agent 入口与图形结构

```text
另外，要工作的子agent的入口在哪里，用户的需求原文+图形结构需要补上
```
